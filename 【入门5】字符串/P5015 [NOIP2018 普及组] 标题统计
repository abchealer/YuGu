

#include<iostream>
#include <string.h>
#include <cstdio>
using namespace std;
string s;
int main(){
	getline(cin,s);//用getline函数读入一行
	int res=0;
	for(int i=0;i<s.size();i++){
		if(s[i]==' '||s[i]=='\n')
		res=res;
		else res++;
	}
	printf("%d", res);
	
	
	return 0;
}
